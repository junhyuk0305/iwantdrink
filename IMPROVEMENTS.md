# iWantDrink — 기획안 갭 분석 & 개선 제안서

> 작성일: 2026-05-20
> 기준: [text.md](text.md) (v2 기획안) ↔ 현재 `app/` 구현
> 목적: ① 기획안 대비 미구현/부분 구현 항목 도출 ② 전문 서비스 기획자 + 실사용자 시각에서의 피드백 ③ 다음 라운드 우선순위 제안

---

## 1. 기획안 ↔ 구현 매핑 (한눈에)

| 기획안 항목 (text.md) | 구현 상태 | 비고 |
|---|---|---|
| §4-① 잔 채우기 / 원샷 | ✅ 완료 | [GlassStage](app/src/ui/GlassStage.jsx), [Glass](app/src/ui/Glass.jsx), [OverflowDrip](app/src/ui/OverflowDrip.jsx) |
| §4-② 잔/술/장소 자유 변경 | ✅ 완료 (단, 사이드바 → 하단 탭바로 피벗) | [BottomTabBar](app/src/ui/BottomTabBar.jsx) |
| §4-③ 떠다니는 토로 — DB 최근 글 + 실시간 구독 | ✅ 완료 (이번 라운드) | [AmbientChatter](app/src/ui/AmbientChatter.jsx), [subscribeAmbient](app/src/lib/supabase.js#L163) |
| §4-④ 비로그인 3잔 제한 + LoginGate | ✅ 완료 | [LoginGate](app/src/ui/modal/LoginGate.jsx), [useDrinkStore.shoot](app/src/store/useDrinkStore.js) |
| §4-⑤ 설문 → 술 보상 (역할 분기 + 4병) | ✅ 완료 | [SurveyModal](app/src/ui/modal/SurveyModal.jsx), [surveys.js](app/src/data/surveys.js) |
| §4-⑥ 인벤토리 | ✅ 완료 (단, 표시는 술별 카운트만) | [MeModal](app/src/ui/modal/MeModal.jsx), [adjustInventory](app/src/lib/supabase.js) |
| §4-⑦ 따라주기 (답글) | ⚠️ **부분 구현** | 텍스트 게시는 OK. **"잔이 기울어지며 따라지는 SVG 애니메이션" 미구현** |
| §4-⑧ 피드 + 필터 + 무한 스크롤 | ⚠️ **부분 구현** | 50개 제한, **무한 스크롤 미구현**, **필터는 장소만 노출**(잔/술별 필터는 API만 존재) |
| §4-⑨ 통계 — 잔×술 / 세대 / 장소 3탭 | ⚠️ **부분 구현** | RPC 호출 OK. 단 **표시가 `question_id` 그대로**(`work-j-1` 등) → 사람이 읽기 어려움 |
| §4-⑩ 매직링크 로그인 | ✅ 완료 | [LoginModal](app/src/ui/modal/LoginModal.jsx), [sendMagicLink](app/src/lib/supabase.js) |
| §5 데이터 모델 — `profiles` 테이블 | ❌ **미사용** | `display_name`/`role`을 `profiles`에서 안 읽음. `role`은 zustand에만 저장 |
| §5 데이터 모델 — `pours_received` 트리거 | ❓ 확인 필요 | 클라이언트는 select만, 카운트 증가는 DB 트리거 의존 (SETUP.md 검증 필요) |
| §7 PourReply 애니메이션 | ❌ 미구현 | 작은 SVG 잔이 카드 위로 기울어지며 따라지는 액션 없음 |
| §7 보상 즉시 토스트 | ⚠️ 모달 내 DoneView로만 표시 | "1초 안에 토스트" 톤이 아님 |
| §8 RLS rate-limit | ❌ 미구현 | 클라이언트/서버 모두 |
| §9 Phase C — 알림(내 잔에 누가 따라주면) | ❌ Phase C 미시작 | |
| §9 Phase C — 모더레이션 | ❌ Phase C 미시작 | |
| §9 Phase C — OAuth (Google/Kakao) | ❌ Phase C 미시작 | |
| §9 Phase C — 텍스트 임베딩 클러스터링 | ❌ Phase C 미시작 | |
| §9 Phase C — 답글 감사 액션 | ❌ Phase C 미시작 | |
| §11 비로그인 → 로그인 시 인벤토리 마이그레이션 | ❌ **미구현 (위험)** | `client_id` 적립분이 로그인해도 `auth_id`로 자동 이전되지 않음 → **유저 자산 손실 위험** |

### 1.1 부분 구현 상세 — "체크는 됐지만 절반"

- **§4-⑦ 따라주기**: 핵심 인터랙션인 *"작은 SVG 잔이 원글 카드 위로 기울어지며 따라지는 액션"*([PourComposer](app/src/ui/modal/FeedModal.jsx#L194)에 없음) 이 빠지면 단순 댓글창과 다를 게 없음. **이 서비스 정체성의 핵심 메타포**가 시각화되지 않은 셈.
- **§4-⑧ 피드 필터**: `fetchFeed({ venueId, glassId, drinkId })`는 API가 다 받지만 [FeedModal](app/src/ui/modal/FeedModal.jsx#L44-L64)의 칩 UI는 장소만 노출. **잔/술 필터 칩 추가하면 즉시 켜짐.**
- **§4-⑨ 통계**: `question_id`("work-j-1") 그대로 표시 → 일반 사용자는 의미 모름. **`surveys.js`의 prompt를 join해서 표시**해야 함.

---

## 2. 사용자 입장 피드백 (실사용 시 거슬리는 것들)

### 2.1 첫 진입 — 무엇을 해야 하는지 모름
- 첫 화면에 잔만 덩그러니. **"오늘 하루를 부어보세요"** placeholder는 있지만 *왜 부어야 하는지 / 부으면 뭐가 되는지*가 안 보임.
- 다른 사람 글이 떠다니는 건 분위기엔 좋지만, "저게 진짜 누가 쓴 거야?" 의구심이 없어짐. → **온보딩 한 줄 + 떠다니는 글의 출처(익명/실시간) 미세 표시** 필요.

### 2.2 정보 위계가 흐려졌다 (이번 피벗의 부작용)
- 사이드바 → 하단 탭바로 옮긴 건 모바일/공간 측면에선 정답이지만, 결과적으로:
  - **"오늘 남은 잔"** 카운트는 [ChatInput](app/src/ui/ChatInput.jsx#L30-L38) 위 작은 글씨와 탭바 "내 정보" 두 군데로 분산
  - **"비워낸 잔" (현재 12잔 같은 카운터)** 가 메인에서 사라짐 → 게이미피케이션의 핵심 보상(레벨업 느낌)이 약해짐
- → **상단 우측에 작은 "오늘 X잔 / 누적 Y잔" 칩**을 영구 노출하는 게 좋겠음.

### 2.3 모달의 깊이 (탭 → 모달 → 모달)
- 한 잔 더 탭 → MenuModal → SurveyModal/FeedModal — **3단 깊이**.
- MenuModal은 사실상 라우터 역할만 함. → "한 잔 더" 탭을 클릭하면 작은 **액션 시트(설문/피드/통계 버튼 3개를 탭바 위에 펼침)** 만으로도 충분. 한 번의 모달을 줄일 수 있음.

### 2.4 "한 잔 따라주기"의 학습 곡선
- 피드 카드의 `🥃 한 잔 따라주기` 버튼을 처음 보면 무슨 행동인지 직관 안 옴.
- 인벤토리가 비어 있으면 [PourComposer](app/src/ui/modal/FeedModal.jsx#L225-L249)에서 *"가진 술이 없어요, 한 병 풀까요?"* 안내는 좋음. 하지만 **인벤토리가 있어도 "이게 댓글이라는 것"** 을 사용자에게 알려주는 카피가 없음.
- → 첫 진입 시 한 번만 "**잔이 댓글이 됩니다. 따라주면 답글로 보여요**" 같은 코치마크.

### 2.5 떠다니는 글귀의 자기/타인 구분 부재
- "내가 방금 쓴 글이 떠다닌다 = 다른 사람 글이 떠다닌다"가 시각적으로 동일.
- 본인 글은 살짝 다른 색 또는 "내 글" 배지가 있으면 *"내 글이 진짜 올라갔구나"* 라는 안도감이 생김.

### 2.6 통계 페이지의 가독성
- [StatsModal — QuestionBlock](app/src/ui/modal/StatsModal.jsx#L253-L280)이 `questionId`("work-j-1")를 그대로 출력.
- **사용자는 "이게 무슨 질문이었지?" 매번 머릿속 매핑**해야 함. → `surveys.js`의 prompt를 라벨로 사용.

### 2.7 보상 직후 피드백 약함
- 설문 완료 → DoneView 모달 → "돌아가기" 누르면 메인. 인벤토리에 들어온 게 시각적으로 강조 안 됨.
- 기획안 §7의 **"+1병 애니메이션 + 토스트"** 처럼 메인에서 잔이 잠깐 진동/번쩍하면 좋음.

### 2.8 모바일에서 잔이 너무 큼
- 가로 360px 화면에서 `glass.width`가 그대로 적용되면 잔이 화면 가로의 70% 차지. 모달 진입 시 답답함.
- → 잔 사이즈를 viewport에 비례시키는 반응형 스케일(예: `min(180px, 40vw)`).

### 2.9 비로그인 → 로그인 시 인벤토리 손실 (치명적)
- localStorage의 `client_id`로 쌓은 인벤토리가 로그인 후 `auth_id`로 자동 이전되지 않음.
- 사용자: "**설문 풀고 술 모았는데 로그인하니까 다 사라졌어요**" 시나리오 가능.
- → 로그인 직후 `client_id`의 inventory/survey_answer를 `auth_id`로 MERGE하는 마이그레이션 RPC 필요.

---

## 3. 서비스 기획자 입장 피드백

### 3.1 비즈니스 모델의 출구 (Exit) 미정의
- 기획안 §2는 "운영자/연구자가 슬라이스 가능한 정형 데이터"를 가치로 둠.
- 하지만 **운영자가 그 데이터를 어디서/어떻게 보는지 정의가 없음.** 클라이언트의 StatsModal은 사용자용. → 별도 **운영자 대시보드 (Supabase Studio 직링크 또는 별도 admin 페이지)** 가 필요.
- 데이터 export(CSV, JSON), 기간 필터, 익명화 수준 정책도 빈자리.

### 3.2 정량 데이터의 결 — 5지선다만으로는 얕다
- 4병 × 5문항 = 20문항. 사람이 쌓는 데이터는 *"포차에서 소주잔에 참이슬을 마시며 (text) 라고 토로했다"* 라는 **풍부한 정성 텍스트**.
- 그런데 통계 페이지는 5지선다 응답 빈도만 보여줌. **원글 텍스트와 5지선다 답의 연결이 분석되지 않음.**
- → Phase C 임베딩 클러스터링이 우선순위 높음. 또는 더 빠르게: **5지선다 응답별로 그 사용자가 쓴 원글 일부 미리보기**를 같이 보여주면 정성↔정량 다리가 생김.

### 3.3 알코올 메타포의 보편성 문제
- 회복 중인 알코올 의존자, 종교(무슬림 등), 임산부, 음주 미경험자에게는 거리감.
- 직장 스트레스라는 주제에서 **음주를 권장하는 톤**으로 읽힐 위험 — *"술로 푼다"* 같은 5지선다 항목까지 있음.
- → **"무알콜 모드" / "차·음료 모드" 토글** 또는 메타포를 *"잔 비우기"* 정도로 중성화. (현재도 잔 = 발화 메타포이긴 함. 술 이름을 *"오늘의 한 잔"* 같이 추상화 가능.)

### 3.4 분기가 너무 단순 — 주니어/시니어 이분법
- 기획안 §10의 ROLE_PROBE는 5단계인데, [surveys.js:7](app/src/data/surveys.js#L7)에서 0~2 → junior, 3~4 → senior로 압축됨.
- 산업(IT/제조/서비스/공공), 직군(개발/디자인/영업/HR), 회사 규모(스타트업/대기업)에 따라 어려움의 결이 다름.
- → 최소 **산업 + 직군** 2축은 첫 진입 시 1회 더 수집. 통계의 슬라이스 축이 4축(잔/술/세대/장소) → 6축으로 늘어남.

### 3.5 데이터 신뢰성 — 보상이 술이라 가벼운 답이 쌓일 위험
- 5문항 후 술 보상 = 응답률은 오르지만 **무성의한 답변(클릭만)이 섞임.**
- 완화책:
  - 같은 답만 5개 연속 = 패널티 (보상 0)
  - 응답 시간 너무 짧음(< 1초) = 보상 0
  - 같은 사용자가 같은 병 반복 풀이 시 보상 1회로 캡 (현재 UNIQUE 제약은 있음)
- 데이터 사이언스 표준의 *"attention check"* 한 문항 추가도 고려.

### 3.6 안전망 (Safety net) 부재 — 직장 스트레스 서비스의 필수
- 음주·자해·자살 신호가 담긴 글이 떠다닐 위험.
- 모더레이션이 Phase C로 미뤄져 있는데, **이건 Phase B 이전에 있어야 하는 문제**임.
- 최소 대응:
  1. 위험 키워드(자살/자해 단어) 매칭 시 client-side에서 **상담 안내**(보건복지부 1577-0199, 청년상담 1388 등) 모달
  2. 작성된 글이 떠다니거나 피드에 노출되기 전 큐(`status='pending'`) → admin 승인
  3. 사용자가 글 신고 가능

### 3.7 운영지표 (Metric) 정의가 비어 있음
- 이 서비스의 성공은 어떤 숫자로 측정하나? 기획안 §2의 "정량 데이터"는 *수단*. *목표* 지표가 없음.
- 후보:
  - 일일 활성 사용자(DAU), 글 생성률, 답글률(따라주기 비율)
  - 설문 응답률(설문 시작 → 1병 완료 비율)
  - 응답 품질 (위 attention check 통과율)
  - 7일 retention (직장인 한 주 사이클 기준)
- → 이걸 정해야 *"어디를 개선했을 때 진짜 잘된 건지"* 판단할 수 있음.

### 3.8 익명 vs 책임 — 사회적 책임의 균형
- 익명이라서 발화 안전 ↔ 익명이라서 무책임한 발언/특정 회사·인물 명예훼손 가능.
- → 글 작성 시 가벼운 안내문("이 글은 익명이지만 공개됩니다. 회사명/실명은 자동 마스킹됩니다") + 회사명/실명 자동 마스킹 패턴(정규식 단계로 가능).

### 3.9 정보 구조 — "한 잔 더" 라는 명명의 모호함
- 현재 탭바의 "한 잔 더" 안에 설문/피드/통계가 묶여 있음. **이 셋은 성격이 다름**:
  - 설문 = 능동적 데이터 제공
  - 피드 = 수동적 소비 + 상호작용
  - 통계 = 자기 위치 비교
- → 정보 구조 재설계: 탭바를 [장소·잔][피드][설문][내 정보] 4개로. "한 잔 더"는 액션 동사라 IA 카테고리로 부적합.

### 3.10 컨버전 퍼널이 너무 길다
- 사용자가 통계까지 도달 = [원샷 3잔] → [LoginGate] → [SurveyModal 5문항] → [메인] → [MenuModal] → [StatsModal]. 6 step.
- 그런데 **통계가 사실 이 서비스의 운영자 가치의 핵심**.
- → 통계의 일부(예: 본인이 답한 항목의 전체 분포)를 LoginGate 이전에도 살짝 미리보여서 *"내가 답하면 이런 화면이 풍부해진다"* 라는 호기심 미끼.

---

## 4. 우선순위별 개선 제안

### 🔴 P0 — 즉시 (다음 라운드)
| # | 항목 | 이유 | 예상 작업량 |
|---|---|---|---|
| 1 | **비로그인→로그인 인벤토리 마이그레이션** RPC | 자산 손실 위험 (치명적) | 0.5d (Supabase RPC + 클라이언트 1회 호출) |
| 2 | **위험 키워드 → 상담 안내 모달** | 사회적 책임 | 0.5d (정규식 + 모달) |
| 3 | **통계의 `question_id` → `prompt` 라벨** | 통계 가독성 | 0.5d (surveys.js 룩업 추가) |
| 4 | **피드 잔/술 필터 칩 UI** | API는 이미 됨, UI만 노출 | 0.3d |
| 5 | **상단 우측 영구 카운터 칩** ("오늘 X / 누적 Y") | 게이미피케이션 약화 회복 | 0.3d |

### 🟡 P1 — 다음다음 라운드
| # | 항목 | 이유 | 예상 작업량 |
|---|---|---|---|
| 6 | **따라주기 시 SVG 잔 기울기 애니메이션** | 서비스 정체성 메타포 살리기 | 1.0d |
| 7 | **피드 무한 스크롤 (페이지네이션)** | 데이터 누적되면 50개 한계 발견됨 | 0.5d |
| 8 | **떠다니는 글의 본인 글 시각 구분** | 작성 피드백 강화 | 0.3d |
| 9 | **MenuModal 제거 → 액션 시트** | 모달 깊이 1단 축소 | 0.5d |
| 10 | **회사명/실명 자동 마스킹 정규식** | 명예훼손/개인정보 리스크 완화 | 0.5d |
| 11 | **잔/입력창 viewport 비례 스케일** | 좁은 모바일 답답함 | 0.3d |
| 12 | **profiles 테이블 활용 (display_name, role 저장/조회)** | 데이터 모델 일관성 | 1.0d |

### 🟢 P2 — Phase C 본격 시작 전 정해둘 것
| # | 항목 | 의사결정 필요 |
|---|---|---|
| 13 | **알림 (내 잔에 누가 따라줬을 때)** | in-app vs 이메일 vs 둘 다, 빈도 |
| 14 | **모더레이션 파이프라인** | client filter / edge function / admin queue 중 어디까지 |
| 15 | **OAuth (Google/Kakao)** | 매직링크와 병행 vs 대체 |
| 16 | **임베딩 기반 클러스터링** | OpenAI vs Supabase pgvector + 자체 임베딩 |
| 17 | **운영자 대시보드** | Supabase Studio 권한 vs 별도 admin SPA |
| 18 | **산업/직군 축 추가** | 첫 진입 코스트 vs 슬라이스 풍부함 |
| 19 | **무알콜 모드** | 토글 vs 톤 자체 중성화 |
| 20 | **운영지표 정의** (DAU, 응답률, retention) | PostHog/Plausible 등 도입 여부 |

### 🔵 P3 — 데이터 정합 / 운영 도구
| # | 항목 | 메모 |
|---|---|---|
| 21 | `pours_received` 트리거 SETUP.md 검증 | 클라이언트는 select만 함, DB 트리거가 있어야 정확 |
| 22 | rate-limit (Supabase Edge Function 또는 클라이언트 큐) | 스팸 방지 |
| 23 | attention check 문항 | 데이터 신뢰성 |
| 24 | 데이터 export (CSV/JSON) | 운영자/연구자 요구 |

---

## 5. 의사결정이 필요한 트레이드오프

다음 라운드 시작 전에 결정해두면 좋은 것들:

1. **음주 메타포의 진폭** — 현재 "참이슬/카스/잭다니엘" 같은 브랜드명도 그대로. 일반 사용자/B2B 채택을 노릴 거면 *"맑은 잔/거품 잔/짙은 잔"* 같은 추상화 검토.
2. **답글 = 따라주기**의 비중 — 현재 답글 인터랙션은 텍스트 중심. 기획안의 *"SVG 잔이 기울어지며 따라지는"* 비주얼이 빠지면, 일반 댓글 서비스와의 차별점이 약함.
3. **통계의 공개 범위** — 사용자별로 본인 응답분만 보여줄 것인가, 익명 집계는 모두에게 공개할 것인가. 현재는 후자. **이게 데이터 수집 동기에 큰 영향.**
4. **3잔 / 1병=5잔 숫자의 합리성** — 비로그인은 3잔, 설문 1병 = 5잔 → 즉 비로그인도 설문만 풀면 사실상 8잔. "보상이 너무 많다 → 글이 가벼워진다" vs "보상이 적다 → 응답률 하락"의 균형.

---

## 6. 한 줄 요약

> 기획안 Phase B는 **기능 차원에서는 거의 다 들어왔지만, 서비스의 정체성을 만드는 "따라주는 잔" 비주얼·정량/정성 데이터의 연결·안전망**이 비어 있다. 다음 라운드의 P0는 *기능 추가가 아니라 **신뢰**의 보강*(인벤토리 이전 / 위험 신호 안내 / 통계 가독성)에 집중해야 한다.
